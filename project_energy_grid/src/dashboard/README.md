# Dashboard

Run the completed geographic and analytical dashboard from the project root:

```bash
streamlit run src/dashboard/app.py
```

The displayed risk score is an operational risk proxy, not a confirmed outage
or network-failure prediction.
