# Models

This package contains the forecasting, evaluation, backtesting, course-method,
and operational risk proxy implementations used by the project scripts.

The risk score is an explainable operational proxy, not a prediction or
probability of real network failure. Labelled outage/failure events are not
available in the project data.
